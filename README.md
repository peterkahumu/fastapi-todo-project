# FastAPI To-Do API  

A simple To-Do API built with **FastAPI**. It allows users to create, read, update, and delete tasks. The app uses Pydantic models for data validation and FastAPI’s automatic docs for quick testing and visualization.  

## 🚀 Features  
- View all to-dos or fetch only the first few  
- Retrieve a single to-do by its ID  
- Create new to-dos with validation  
- Update existing to-dos safely  
- Delete tasks easily  

## 🧠 Tech Stack  
- **Language:** Python 3.10+  
- **Framework:** FastAPI  
- **Server:** Uvicorn  
- **Validation:** Pydantic  

## 📦 Installation  

1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/fastapi-todo.git
   cd fastapi-todo
    ```

2. **Create a virtual environment**

   ```bash
   python -m venv venv
   source venv/bin/activate   # On Windows: venv\Scripts\activate
   ```

3. **Install dependencies**

   ```bash
   pip install fastapi uvicorn
   ```

4. **Run the application**

   ```bash
   uvicorn main:api --reload
   ```

5. **Open the browser**

   ```
   http://127.0.0.1:8000/docs
   ```

   You’ll see the interactive Swagger documentation auto-generated by FastAPI.

## 📘 API Endpoints

| Method | Endpoint           | Description        |
| ------ | ------------------ | ------------------ |
| GET    | `/todos`           | Get all to-dos     |
| GET    | `/todo/{todo_id}`  | Get a single to-do |
| POST   | `/todos/`          | Create a new to-do |
| PATCH  | `/todo/{todo_id}/` | Update a to-do     |
| DELETE | `/todo/{todo_id}/` | Delete a to-do     |

## 🧩 Example

```bash
# Create a new to-do
curl -X POST "http://127.0.0.1:8000/todos/" -H "Content-Type: application/json" -d '{"name": "Study FastAPI", "description": "Learn the basics of FastAPI", "priority": 2}'
```

Response:

```json
{
  "todo_id": 10,
  "name": "Study FastAPI",
  "description": "Learn the basics of FastAPI",
  "priority": 2
}
```

## ⚙️ Known Issues

* Since all data is stored in memory, all tasks reset when the app restarts.
* No database connection yet.

## 🧭 Future Improvements

* Add database support (SQLite or PostgreSQL)
* Add authentication and user-based tasks
* Add pagination and filtering

## 📚 References

* [FastAPI Official Docs](https://fastapi.tiangolo.com)
* [Youtube FastAPI Crash course](https://www.youtube.com/watch?v=rvFsGRvj9jo)

